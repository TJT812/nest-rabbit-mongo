enum LogLevelEnum {
  info = 'info',
  debug = 'debug',
  error = 'error',
  warn = 'warn',
}

enum StageEnum {
  prod = 'prod',
  dev = 'dev',
}

const servicesEnum = {
  NOTIFICATION_SERVICE: 'NOTIFICATION_SERVICE',
};

export { LogLevelEnum, StageEnum, servicesEnum };
